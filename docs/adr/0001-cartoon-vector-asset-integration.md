# 0001 Cartoon Vector Asset Integration

We decided to use open vector asset APIs and CDNs (e.g. DiceBear vector API and open game asset CDNs) to dynamically deliver animated cartoon characters, cargo pods, and collectible items across learning modes. This provides crisp, high-resolution vector artwork that scales across all screen sizes without bloating the local repository bundle with heavy raster art.
